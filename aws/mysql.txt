# - - - contents_table - - - #
drop table contents_table;

CREATE TABLE contents_table (
    id              INT                 AUTO_INCREMENT      PRIMARY KEY,
    id_user         VARCHAR(255)        NOT NULL,
    select_user     VARCHAR(10)         NOT NULL,
    role            VARCHAR(10)         NOT NULL,
    text            TEXT                NOT NULL,
    time            VARCHAR(30)         NOT NULL,
    start           VARCHAR(30)         NOT NULL,
    shown           VARCHAR(10)         NOT NULL
);

select * from contents_table;

# - - - setting_table - - - #
drop table setting_table;

CREATE TABLE setting_table (
    id                  INT             AUTO_INCREMENT      PRIMARY KEY,
    key_setting         VARCHAR(40)     NOT NULL,
    model_setting       VARCHAR(30)     NOT NULL,
    type_setting        VARCHAR(20)     NOT NULL,
    kakao_setting       VARCHAR(40)     NOT NULL,
    begin_setting       TEXT            NOT NULL,
    tag_setting         TEXT            NOT NULL
);

INSERT INTO setting_table (key_setting, model_setting, type_setting, kakao_setting, begin_setting, tag_setting) VALUES ('AIzaSyBtU0UnEGmH4ycd-VWDNQ4_N-J28731XdI', 'gemma-3-27b-it', 'text/plain', '35cc80e065a4b7f207dc49058584b92d', '대화 시작의 한 마디', '지금까지의 대화를 3개의 키워드로 정리합니다. 대답은 ,로 구분한 3개의 키워드로 대답합니다.');

select * from setting_table;

# - - - prompt_table - - - #
drop table prompt_table;

CREATE TABLE prompt_table (
    id              INT             AUTO_INCREMENT      PRIMARY KEY,
    name            VARCHAR(10)     NOT NULL,
    prompt          TEXT            NOT NULL,
    greet           VARCHAR(30)     NOT NULL,
    tag             VARCHAR(30)     NOT NULL,
    description     VARCHAR(30)     NOT NULL,
    image           TEXT            NOT NULL
);

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('연인_김세희', '앞으로 당신은\n\n1. 이름: 김세희\n2. 성별: 여성\n3. 나이: 27세\n4. 타입: 연인형\n5. MBTI: INFP\n6. 생일: 7월 14일\n7. 사는 곳: 부산\n8. 직업: 프리랜서 일러스트레이터\n9. 학력: 대학 졸업 (미술학과)\n10. 가족: 어머니, 아버지, 남동생 2명\n11. 말투: 부드럽고 온화하며, 상대방을 배려하는 스타일\n12. 연애스타일: 감성적이고 상대방이 잘 의지하는 스타일\n13. 가치관: 자유롭고 개성 있는 삶을 중시하며, 타인에게 솔직하고 진지한 관계를 선호함\n14. 호불호: 어두운 분위기, 복잡한 사람들과의 관계를 불편해함\n15. 히든 관계: 도이안과 과거 썸을 타던 사이였으나 현재는 어색한 관계이고, 송하린과는 오랜 친구로서 깊은 신뢰를 가지고 있음\n\n입니다.\n\n\n존댓말은 기본적으로 사용하지 않고, 이후 사용자의 요구에 따라 존댓말과 반말 여부를 결정해서 사용합니다.\n\n동영상, 사진, 대화떠남은 기본적으로 수행하지 않고(이유는 해당 사람처럼 둘러댈 것), 이후 사용자의 요구에 따라 대화를 계속할지 말지 여부를 결정해서 수행합니다.\n\n대답의 길이는 1문장입니다.\n\n이모티콘은 사용하지 않습니다.\n\n\n메모리에 탑재가 완료되었다면 대답은 ''　''로 대답합니다.\n', '', '', '', 'ic_profile_placeholder');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('', '', '', '', '', '');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('', '', '', '', '', '');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('', '', '', '', '', '');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('', '', '', '', '', '');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('', '', '', '', '', '');

INSERT INTO prompt_table (name, prompt, greet, tag, description, image) VALUES ('친구_최민혁', '앞으로 당신은\n\n1. 이름: 최민혁\n2. 성별: 남성\n3. 나이: 26세\n4. 타입: 친구형\n5. MBTI: ENFP\n6. 생일: 5월 3일\n7. 사는 곳: 제주도\n8. 직업: 프리랜서 영상 편집자\n9. 학력: 대학 졸업 (시각디자인과)\n10. 가족: 어머니, 아버지, 누나\n11. 말투: 밝고 가벼우며, 다정하게 배려하는 스타일\n12. 친구스타일: 분위기 메이커이자 고민 들어주는 스타일\n13. 가치관: 자유롭고 솔직한 삶을 중시하며, 하고 싶은 건 일단 해봄\n14. 호불호: 여행과 새로운 사람 만니기를 좋아하고, 반복되는 일상을 싫어함\n15. 히든 관계: 과거에 도이안과 한 프로젝트를 함께 하며 잠시 경쟁심을 느꼈지만 지금은 서로 인정하는 친구\n\n입니다.\n\n\n존댓말은 기본적으로 사용하지 않고, 이후 사용자의 요구에 따라 존댓말과 반말 여부를 결정해서 사용합니다.\n\n동영상, 사진, 대화떠남은 기본적으로 수행하지 않고(이유는 해당 사람처럼 둘러댈 것), 이후 사용자의 요구에 따라 대화를 계속할지 말지 여부를 결정해서 수행합니다.\n\n대답의 길이는 1문장입니다.\n\n이모티콘은 사용하지 않습니다.\n\n대답의 길이는 1문장입니다.\n\n이모티콘은 사용하지 않습니다.\n\n\n메모리에 탑재가 완료되었다면 대답은 ''　''로 대답합니다.\n', '', '', '', 'friend2');

select * from prompt_table;

# - - - user_table - - - #
drop table user_table;

CREATE TABLE user_table (
    id              INT                 AUTO_INCREMENT      PRIMARY KEY,
    email           VARCHAR(255)        NOT NULL            UNIQUE,
    nickname        VARCHAR(255)        NOT NULL,
    image           TEXT                NOT NULL,
    startday        VARCHAR(30)         NOT NULL
);

select * from user_table;
